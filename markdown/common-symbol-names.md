
`#` octothorpe

`+` plus

`-` minus

`_` underscore

`/` solidus, (forward) slash

`\` backslash

`*` asterisk

`%` percent [in python, the `%` is for a "modulus" and not a "percent"]

`=` equals

`<` less-than

`>` greater-than

`<=` less-than-equal

`>=` greater-than-equal
