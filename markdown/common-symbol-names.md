
`#` octothorpe

`+` plus
`-` minus
`/` slash
`*` asterisk
`%` percent [in python, the `%` is for a "modulus" and not a "percent"]
`<` less-than
`>` greater-than
`<=` less-than-equal
`>=` greater-than-equal
